import { UserTradesPipe } from './user-trades.pipe';

describe('UserTradesPipe', () => {
  it('create an instance', () => {
    const pipe = new UserTradesPipe();
    expect(pipe).toBeTruthy();
  });
});
