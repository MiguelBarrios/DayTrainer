import { SectorPipePipe } from './sector-pipe.pipe';

describe('SectorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SectorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
